# State files — generated, not committed
data.jsonl
quarantine.jsonl
final_output.jsonl
alerts.jsonl
metrics.txt

# Markers — multi-agent coordination
markers/*.lock
markers/*.done

# Temp files
*.tmp
*.bak

# OS
.DS_Store

# Marketing — private drafts
marketing/

# Launch silo — working directory, not committed
silo_launch/
.todos/
.sidecar/
.sidecar-agent
.sidecar-task
.sidecar-pr
.sidecar-start.sh
.sidecar-base
.td-root
node_modules/
