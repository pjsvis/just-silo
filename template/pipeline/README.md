# template/pipeline/

Pipeline configuration files. Used by `process_harvest.sh` for data processing workflows.

- `pipeline.json` — Pipeline definition
- `schema.json` — Input/output validation
- `queries.json` — Named jq filters
