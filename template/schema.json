{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Grain Elevator Sensor Reading",
  "description": "Canonical structure for a single moisture sensor reading from a grain elevator.",
  "type": "object",
  "required": ["timestamp", "elevator_id", "moisture"],
  "properties": {
    "timestamp": {
      "type": "string",
      "description": "ISO 8601 timestamp of the reading"
    },
    "elevator_id": {
      "type": "string",
      "description": "Unique identifier for the grain elevator"
    },
    "moisture": {
      "type": "number",
      "description": "Moisture percentage. >15% is a critical alert.",
      "minimum": 0,
      "maximum": 100
    },
    "status": {
      "type": "string",
      "description": "Processing status: pending | processed | exported | quarantined",
      "enum": ["pending", "processed", "exported", "quarantined"]
    }
  },
  "additionalProperties": false
}
